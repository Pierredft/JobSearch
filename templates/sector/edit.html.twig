{% extends 'base.html.twig' %}

{% block title %}Modifier secteur - Job Search ERP{% endblock %}

{% block body %}
<div class="fade-in">
    <div class="flex-between mb-xl">
        <h1 class="page-title mb-0">✏️ Modifier secteur</h1>
        <a href="{{ path('app_sector_index') }}" class="btn btn-secondary">
            ← Retour
        </a>
    </div>

    <div class="card" style="max-width: 600px; margin: 0 auto;">
        {{ include('sector/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="mt-xl pt-lg" style="border-top: 1px solid var(--border-color);">
            <h3 class="text-danger mb-md">Zone de danger</h3>
            {{ include('sector/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
