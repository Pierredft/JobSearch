{% extends 'base.html.twig' %}

{% block title %}Nouvelle Entreprise - Job Search ERP{% endblock %}

{% block body %}
<div class="fade-in">
    <h1 class="page-title">➕ Nouvelle Entreprise</h1>

    <div class="card" style="max-width: 600px;">
        {{ form_start(form) }}
        
        <div class="form-group">
            {{ form_label(form.name) }}
            {{ form_widget(form.name) }}
        </div>

        <div class="form-group">
            {{ form_label(form.website) }}
            {{ form_widget(form.website) }}
        </div>

        <div class="form-group">
            {{ form_label(form.industry) }}
            {{ form_widget(form.industry) }}
        </div>

        <div class="form-group">
            {{ form_label(form.location) }}
            {{ form_widget(form.location) }}
        </div>

        <div class="flex gap-md" style="margin-top: 2rem;">
            <button type="submit" class="btn btn-primary">💾 Enregistrer</button>
            <a href="{{ path('app_company_index') }}" class="btn btn-secondary">Annuler</a>
        </div>

        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
